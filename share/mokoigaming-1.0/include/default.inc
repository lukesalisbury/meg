#include <game>
#include <graphics>
#include <entities>
#include <fixed>
#include <string>
#include <map>

#tryinclude <game_default>
#include <controller>

forward public Init(...);
forward public UpdatePosition();
forward public Close();

/*
forward public @PreInit();
forward public @PostInit();
forward public @PreClose();
forward public @PostClose();
forward public @UpdatePosition();
*/